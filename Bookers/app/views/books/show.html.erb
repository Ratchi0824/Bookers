
<%= flash[:notice] %>
<%= form_for(@book) do |f| %>
<p> Title: <%= @book.title %></p>
<p> Body: <%= @book.body %></p>
  <%= link_to "Edit", edit_book_path(@book.id)%>
  "|"
  <%= link_to "Back", books_path%>
<% end %>